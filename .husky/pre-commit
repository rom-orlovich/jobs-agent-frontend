# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# tsc --noEmit && npm run eslint . && npm run prettier --write .
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged # Replace the last line with "yarn lint-staged"
